using UnityEngine;
using UnityEngine.UI;

public class score : MonoBehaviour
{
    //public RobotPosition RobotPosition;
    public Text scoreText;

    // Update is called once per frame
    void Update()
    {
        //scoreText.text=robot.transform.position.ToString();
    }
}
